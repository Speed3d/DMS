# سجلّ الإصدارات — نظام DMS

> **الرقم في ملف `VERSION` وحده** (ADR-054) — ويقرؤه الخادم والواجهة معاً، ويظهر في شاشة الدخول
> وأسفل القائمة الجانبية و«حول النظام». لكل إصدارٍ هنا قسم، **ووسمٌ في git** (`v0.9.0`).
>
> | يتغيّر | متى |
> |---|---|
> | **الإصلاح** `x.y.Z` | إصلاح عطلٍ بلا ميزةٍ جديدة |
> | **الفرعي** `x.Y.0` | ميزةٌ جديدة **أو مهاجرةُ قاعدة بيانات** |
> | **الرئيسي** `X.0.0` | تغييرٌ لا يتوافق مع ما قبله |
>
> ⚠️ **`0.9.x` قبل الإنتاج، و`1.0.0` يوم تشغيل الـMini PC** (قرار المالك 2026-09-24).
> والتفصيل التقنيّ لكل بند في `.claude/docs/decisions.md` برقم الـADR.

---

## [0.9.0] — 2026-09-25 — أوّلُ إصدارٍ مرقَّم

**أوّلُ رقمٍ للبرنامج** — يجمع كلَّ ما بُني حتى اليوم. قاعدة البيانات: **31 مهاجرة** (آخرُها `AddClientRequests`).

### الوحدات (اثنتا عشرة)
الصادر · الوارد · الأقسام · الأرشيف · المستخدمون والصلاحيات · التقارير (مالي · نشاط · صادر تفصيلي ·
أرشيف تفصيلي · مهام) · النسخ الاحتياطي والاستعادة والمرآة · الأمان والتدقيق · الموظفون والرواتب ·
المهام (مع لوحة كانبان وإشعارات وخدمة خلفية) · المعاملات (خيطُ مراسلاتٍ يجمع الوارد والصادر) ·
والبروفايل الشخصي. **وختمُ QR** يُتحقَّق منه بكاميرا الهاتف عبر صفحةٍ عامّة.

### أُضيف في الأسابيع الأخيرة
- **إيقاف النظام للصيانة وشريط الإعلان** (ADR-050) — السوبر أدمن وحده يتصفّح أثناء الإيقاف، وأمر طوارئ على السيرفر.
- **حماية ما يُكتب** (ADR-051) — مسوّداتٌ تُحفظ أثناء الكتابة ولا تضيع بتوقّف الخادم، و«مسوّداتي»، ومنعُ الإنشاء مرّتين.
- **العمليات الطويلة خارج الطلب** (ADR-049) — النسخ والاستعادة وحذف الشركة لا يقطعها Cloudflare.
- **رقم الإصدار** (ADR-054) — هذا السجلّ، و«حول النظام»، وتنبيهُ السوبر أدمن إن اختلف إصدار الواجهة عن الخادم.

### أُصلح في الأسابيع الأخيرة
- الكلمة المؤقتة يفرضها الخادم لا الواجهة وحدها، والتغيير يُنهي الجلسات الأخرى (G19 — ADR-052).
- سنة الترقيم بتوقيت بغداد — كتابُ فجر 1 يناير كان يأخذ السنة السابقة (G21 — ADR-052).
- رقمُ الكتاب المرتبط لا يظهر لمن لا يملك قسمه — يُعلَن بالعدد (G20 — ADR-053).
- القارئ لا يعتمد ولا يدير الوارد (G22 — ADR-053).
- بيانُ حذف الشركة يعدّ كلَّ ما سيُمحى (G23 — ADR-053).
- زرّ «نسيت كلمة المرور» يعمل، وشاشة الدخول لا تفيض على الهاتف (ADR-053).
- توقّفُ الخادم لم يعُد يُخرج المستخدم من النظام (ADR-050).
- تعديلُ مديرٍ لمستخدمٍ لم يعُد يُسقط صلاحياته في الشركات الأخرى (ADR-048).

### التحقّق
بناء 0/0 · اختبارات الوحدة **561** · اختبارات الواجهة **554** · اختبارات التدفّق الشامل (E2E) **سبعة عشر سكربتاً
+ التهيئة بصفر فشل — 914 تحقّقاً**.
